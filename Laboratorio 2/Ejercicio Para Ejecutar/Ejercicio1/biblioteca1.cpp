// Incluye el archivo de cabecera donde se declara la función suma
#include "biblioteca1.h" 

// Definición de la función suma que toma dos enteros, los suma y devuelve el resultado.
int suma(int a, int b) {
    return a + b; // Retorna la suma de los dos enteros.
}
